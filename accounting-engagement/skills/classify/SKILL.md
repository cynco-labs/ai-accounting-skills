---
name: classify
description: Classify lines to COA — substance analysis when standards-aware.
disable-model-invocation: true
---
# /classify

Short name for classification (including standards-aware substance).

## Do this

1. Load and execute **`classify-transactions`**  
   (`bookkeeping-accounting/skills/classify-transactions/SKILL.md`).  
2. Also load `shared/classify-substance.md` for depth rules.  
3. Year-end / “properly” → `classify_depth=standards_aware` and analysis packs under `workpapers/analysis/`.

**Done when:** classify skill Done when is met.

Theme-only shortcuts: **revenue** · **capex** (same job, one theme).

See `shared/slash-surface.md`.
